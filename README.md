# 🥋 Judo Archive

Web-приложение для архива дзюдо с профилями спортсменов, результатами турниров и статистикой.

## Tech Stack

**Backend:**
- Go 1.21+ - основной язык программирования
- Chi v5 - минималистичный HTTP роутер
- PostgreSQL - база данных

**Frontend:**
- Templ - типизированные HTML шаблоны, компилируются в Go код
- HTMX - динамические обновления без написания JavaScript
- Vanilla CSS - стили в отдельных файлах (static/css/)
- Vanilla JS - скрипты в отдельных файлах (static/js/)

**Архитектура:**
- Server-Side Rendering - Go генерирует HTML через Templ
- Partial Updates - HTMX обновляет части страницы без полной перезагрузки
- No SPA - традиционный серверный рендеринг с интерактивностью

## Структура проекта

```
judo-archive/
├── cmd/main.go                     # Точка входа приложения
├── internal/
│   ├── app/                        # Конфигурация приложения
│   ├── handlers/                   # HTTP handlers
│   ├── models/                     # Go структуры данных
│   ├── services/                   # Бизнес логика
│   └── repository/                 # Работа с БД
├── templates/
│   ├── layouts/                    # Базовые layouts
│   ├── pages/                      # Страницы
│   └── components/                 # Переиспользуемые компоненты
└── static/
    ├── css/                        # CSS файлы (style.css - общий, остальные - для страниц)
    └── js/                         # JavaScript файлы
```

## Как это работает

1. Браузер отправляет запрос с атрибутом `hx-get="/endpoint"`
2. HTMX перехватывает клик и делает HTTP запрос на сервер
3. Go handler обрабатывает запрос и рендерит Templ компонент
4. Сервер возвращает готовый HTML фрагмент (не JSON!)
5. HTMX вставляет HTML в указанное место на странице

## Установка и запуск

### Локальная разработка

```bash
# Установить зависимости
go mod download

# Установить Templ CLI
go install github.com/a-h/templ/cmd/templ@latest

# Компилировать Templ шаблоны
templ generate

# Запустить сервер
go run cmd/main.go
```

### Docker

```bash
# Собрать и запустить через Docker Compose
docker compose up --build

# Приложение будет доступно на http://localhost:8080
```

## Основные команды

```bash
# Компиляция Templ шаблонов (НЕ НУЖНО при использовании air)
templ generate

# Запуск сервера без hot reload
go run cmd/main.go

# Hot reload (автоматически генерирует Templ файлы при изменениях)
air
```

**Важно:** При разработке всегда используй `air` для hot reload. Air автоматически запускает `templ generate` при изменении `.templ` файлов.

## Преимущества подхода

- ✅ Один язык (Go) для всей логики приложения
- ✅ Типобезопасность - Templ компилируется в Go код
- ✅ Отличный SEO - полный серверный рендеринг
- ✅ Минимум JavaScript - всего ~14KB (только HTMX)
- ✅ Быстрая разработка - компоненты переиспользуются
- ✅ Простой деплой - один бинарный файл

## Страницы

- **/** - главная страница с поиском и статистикой
- **/judoka/:id** - профиль дзюдоиста с результатами и медалями
- **/tournament/:id** - страница турнира с категориями и результатами
- **/contribute** - форма добавления информации в архив

## Ресурсы

- [Templ Documentation](https://templ.guide/)
- [HTMX Documentation](https://htmx.org/)
- [Chi Router](https://github.com/go-chi/chi)
- [PostgreSQL](https://www.postgresql.org/)
